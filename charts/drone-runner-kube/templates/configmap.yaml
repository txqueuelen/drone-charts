{{- $fullName := include "drone-runner-kube.fullname" . -}}
{{- with .Values.env.configMap }}
---
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ $fullName }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml . | nindent 2 }}
{{- end }}
