apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "drone-kubernetes-secrets.fullname" . }}-test-connection"
  labels:
    {{- include "drone-kubernetes-secrets.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "drone-kubernetes-secrets.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
